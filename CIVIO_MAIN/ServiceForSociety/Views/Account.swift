import SwiftUI

// Removed Account and Authentication UI per product focus (no accounts)
struct Account: View {
    var body: some View {
        Text("Accounts disabled")
            .foregroundColor(ColorTheme.secondaryText)
    }
}
